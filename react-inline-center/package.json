{
  "name": "react-inline-center",
  "version": "1.0.1",
  "description": "Centering inline element",
  "license": "MIT",
  "repository": "ambar/react-inline-center",
  "module": "dist/index.esm.js",
  "main": "dist/index.cjs.js",
  "scripts": {
    "prepare": "npm run build",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "esbuild index.tsx --outfile=dist/index.cjs.js --format=cjs && esbuild index.tsx --outfile=dist/index.esm.js --format=esm"
  },
  "files": [
    "dist"
  ],
  "peerDependencies": {
    "react": "*"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "react": "^18.2.0",
    "react-test-renderer": "^18.2.0"
  }
}
